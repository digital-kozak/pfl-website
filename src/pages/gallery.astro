---
import Layout from '../layouts/Layout.astro';
import Breadcrumb from '../components/Breadcrumb.astro';

const galleryImages = [
  // Hero Images
  { src: "/images/pfl-stargazing.jpg", alt: "Stargazing at Pagosa Forest Lodge", category: "Exterior" },
  { src: "/images/snowy-cabin.jpg", alt: "Snowy cabin at sunset", category: "Exterior" },
  { src: "/images/hottub-snow.jpg", alt: "Hot tub with snow covered mountains", category: "Amenities" },
  { src: "/images/snowy-cabin2.jpg", alt: "Winter cabin view", category: "Exterior" },

  // Drone/Outside Images
  { src: "/images/006-0278 14317 W US Hwy 160.jpg", alt: "Aerial view of cabin", category: "Exterior" },
  { src: "/images/007-0279 14317 W US Hwy 160.jpg", alt: "Cabin exterior with mountain backdrop", category: "Exterior" },
  { src: "/images/008-0280 14317 W US Hwy 160.jpg", alt: "Mountain sunset view", category: "Views" },
  { src: "/images/014-0287 14317 W US Hwy 160.jpg", alt: "Cabin landscape", category: "Exterior" },
  { src: "/images/018-0291 14317 W US Hwy 160.jpg", alt: "Aerial property view", category: "Exterior" },
  { src: "/images/023-0350 14317 W. US Hwy 160.jpg", alt: "Cabin exterior detail", category: "Exterior" },
  { src: "/images/128-0377 14317 W. US Hwy 160.jpg", alt: "Front deck view", category: "Exterior" },

  // Living Room
  { src: "/images/025-Aerial Interior.jpg", alt: "Great room fireplace", category: "Interior" },
  { src: "/images/026-Aerial Interior_1.jpg", alt: "Living room seating area", category: "Interior" },
  { src: "/images/029-Aerial Interior_2.jpg", alt: "Living space with views", category: "Interior" },
  { src: "/images/031-Aerial Interior_3.jpg", alt: "Cozy seating area", category: "Interior" },
  { src: "/images/033-Aerial Interior_5.jpg", alt: "Great room detail", category: "Interior" },
  { src: "/images/036-Aerial Interior_7.jpg", alt: "Dining area", category: "Interior" },
  { src: "/images/037-Aerial Interior_8.jpg", alt: "Living room corner", category: "Interior" },
  { src: "/images/077-0001 14317 W. US Hwy 160.jpg", alt: "Interior living space", category: "Interior" },

  // Kitchen
  { src: "/images/042-102419263.jpg", alt: "Gourmet kitchen", category: "Kitchen" },
  { src: "/images/043-102419320.jpg", alt: "Kitchen appliances", category: "Kitchen" },

  // Master Bedroom
  { src: "/images/054-102419308.jpg", alt: "Master bedroom king bed", category: "Bedrooms" },
  { src: "/images/057-102419305.jpg", alt: "Master bedroom detail", category: "Bedrooms" },
  { src: "/images/058-102419304.jpg", alt: "Master bedroom view", category: "Bedrooms" },
  { src: "/images/060-0006 14317 W. US Hwy 160.jpg", alt: "Master bedroom sitting area", category: "Bedrooms" },

  // Downstairs Queen
  { src: "/images/078-102419273.jpg", alt: "Downstairs queen bedroom", category: "Bedrooms" },
  { src: "/images/081-0009 14317 W. US Hwy 160.jpg", alt: "Queen bedroom detail", category: "Bedrooms" },
  { src: "/images/082-102419269.jpg", alt: "Queen bedroom sitting area", category: "Bedrooms" },

  // Upstairs Bunk
  { src: "/images/092-102419253.jpg", alt: "Upstairs bunk room", category: "Bedrooms" },

  // 4th Bedroom
  { src: "/images/047-102419313.jpg", alt: "4th bedroom", category: "Bedrooms" },

  // Game Room
  { src: "/images/067-102419291.jpg", alt: "Game room view 1", category: "Interior" },
  { src: "/images/069-102419289.jpg", alt: "Game room view 2", category: "Interior" },
  { src: "/images/096-0007 14317 W. US Hwy 160.jpg", alt: "Game room view 3", category: "Interior" },
  { src: "/images/basement-beds.jpg", alt: "Bunk beds in game room", category: "Bedrooms" },

  // Laundry
  { src: "/images/053-102419309.jpg", alt: "Laundry room", category: "Amenities" },

  // Exterior/Web-optimized
  { src: "/images/14317 W Hwy 160 Pagosa Springs-large-007-005-14317 W Hwy 160-1500x1000-72dpi.jpg", alt: "Cabin exterior web", category: "Exterior" },
  { src: "/images/14317 W Hwy 160 Pagosa Springs-large-015-007-14317 W Hwy 160-1500x1000-72dpi.jpg", alt: "Deck web view", category: "Exterior" },
  { src: "/images/14317 W Hwy 160 Pagosa Springs-large-070-072-14317 W Hwy 160-1500x844-72dpi.jpg", alt: "Property web view", category: "Exterior" },
  { src: "/images/14317 W Hwy 160 Pagosa Springs-large-076-073-14317 W Hwy 160-1500x844-72dpi.jpg", alt: "Mountain sunset", category: "Views" },
];

const categories = ["All", "Exterior", "Interior", "Bedrooms", "Kitchen", "Amenities", "Views"];
---

<Layout 
  title="Gallery | Pagosa Forest Lodge" 
  description="View photos of Pagosa Forest Lodge - luxury mountain cabin with hot tub, stunning views, 4 bedrooms. See your Colorado getaway!"
  breadcrumbs={[{ name: "Gallery", url: "/gallery" }]}
>  <!-- Hero -->
  <section class="relative py-32 hero-overlay">
    <div class="absolute inset-0 z-0">
      <img
        src="/images/gallery-hero-winter.jpg"
        alt="Winter landscape with Chimney Rock"
        class="w-full h-full object-cover"
      />
    </div>

    <div class="relative z-10 container-custom text-center text-white">
      <p class="text-sm uppercase tracking-[0.3em] mb-4">Visual Tour</p>
      <h1 class="font-serif text-5xl md:text-7xl">
        <span class="italic">Gallery</span>
      </h1>
    </div>
  </section>

  <!-- Breadcrumb Navigation -->
  <Breadcrumb items={[{ name: "Gallery", url: "/gallery" }]} />

  <!-- Gallery -->
  <section class="section-padding bg-[#faf9f6]">
    <div class="container-custom">
      <div class="text-center max-w-3xl mx-auto mb-12">
        <p class="text-[#c9a227] text-sm uppercase tracking-[0.2em] mb-4">Explore</p>
        <h2 class="font-serif text-4xl md:text-5xl text-[#0f1f0a]"><span class="italic">Photo</span> Gallery</h2>
      </div>

      <!-- Filter Buttons -->
      <div class="flex flex-wrap justify-center gap-3 mb-12">
        {categories.map((category, index) => (
          <button
            class={`filter-btn px-6 py-2 rounded-full text-sm font-medium transition-all duration-300 ${index === 0 ? 'bg-[#0f1f0a] text-white' : 'bg-white text-[#0f1f0a] hover:bg-[#0f1f0a] hover:text-white'}`}
            data-filter={category}
          >
            {category}
          </button>
        ))}
      </div>

      <!-- Gallery Grid -->
      <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4" id="gallery-grid">
        {galleryImages.map((image, index) => (
          <div
            class="gallery-item aspect-square overflow-hidden rounded-lg cursor-pointer group relative"
            data-category={image.category}
            data-index={index}
          >
            <img
              src={image.src}
              alt={image.alt}
              loading="lazy"
              class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-110"
            />
            <div class="absolute inset-0 bg-black/0 group-hover:bg-black/20 transition-colors duration-300"></div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Lightbox -->
  <div id="lightbox" class="fixed inset-0 z-50 hidden bg-black/95 flex items-center justify-center">
    <button id="lightbox-close" class="absolute top-4 right-4 text-white/80 hover:text-white z-10 p-2">
      <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
      </svg>
    </button>

    <button id="lightbox-prev" class="absolute left-4 text-white/80 hover:text-white z-10 p-2">
      <svg class="w-10 h-10" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
      </svg>
    </button>

    <button id="lightbox-next" class="absolute right-4 text-white/80 hover:text-white z-10 p-2">
      <svg class="w-10 h-10" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
      </svg>
    </button>

    <img id="lightbox-img" src="" alt="" class="max-w-[90vw] max-h-[85vh] object-contain" />

    <p id="lightbox-caption" class="absolute bottom-4 left-0 right-0 text-center text-white/80 text-sm px-4"></p>
  </div>

  <script is:inline define:vars={{ galleryImages }}>
    (function() {
      let currentIndex = 0;
      
      function openLightbox(index) {
        currentIndex = index;
        updateLightbox();
        document.getElementById('lightbox').classList.remove('hidden');
        document.body.style.overflow = 'hidden';
      }
      
      function closeLightbox() {
        document.getElementById('lightbox').classList.add('hidden');
        document.body.style.overflow = '';
      }
      
      function nextImage() {
        currentIndex = (currentIndex + 1) % galleryImages.length;
        updateLightbox();
      }

      function prevImage() {
        currentIndex = (currentIndex - 1 + galleryImages.length) % galleryImages.length;
        updateLightbox();
      }
      
      function updateLightbox() {
        const img = document.getElementById('lightbox-img');
        const caption = document.getElementById('lightbox-caption');
        img.src = galleryImages[currentIndex].src;
        img.alt = galleryImages[currentIndex].alt;
        caption.textContent = galleryImages[currentIndex].alt;
      }
      
      // Wait for DOM to be fully loaded
      document.addEventListener('DOMContentLoaded', () => {
        // Filter functionality
        const filterButtons = document.querySelectorAll('.filter-btn');
        const galleryItems = document.querySelectorAll('.gallery-item');
        
        filterButtons.forEach(btn => {
          btn.addEventListener('click', () => {
            const filter = btn.dataset.filter;
            
            // Update active button styling
            filterButtons.forEach(b => {
              b.classList.remove('bg-[#0f1f0a]', 'text-white');
              b.classList.add('bg-white', 'text-[#0f1f0a]');
            });
            btn.classList.remove('bg-white', 'text-[#0f1f0a]');
            btn.classList.add('bg-[#0f1f0a]', 'text-white');
            
            // Filter items
            galleryItems.forEach((item) => {
              const index = parseInt(item.dataset.index);
              const category = galleryImages[index].category;
              if (filter === 'All' || category === filter) {
                item.style.display = 'block';
              } else {
                item.style.display = 'none';
              }
            });
          });
        });
        
        // Gallery item click - event delegation
        const galleryGrid = document.getElementById('gallery-grid');
        if (galleryGrid) {
          galleryGrid.addEventListener('click', (e) => {
            const item = e.target.closest('.gallery-item');
            if (item) {
              const index = parseInt(item.dataset.index);
              openLightbox(index);
            }
          });
        }
        
        // Lightbox controls
        document.getElementById('lightbox-close').addEventListener('click', closeLightbox);
        document.getElementById('lightbox-next').addEventListener('click', nextImage);
        document.getElementById('lightbox-prev').addEventListener('click', prevImage);
        
        // Click outside to close
        const lightbox = document.getElementById('lightbox');
        if (lightbox) {
          lightbox.addEventListener('click', (e) => {
            if (e.target.id === 'lightbox') closeLightbox();
          });
        }
        
        // Keyboard navigation
        document.addEventListener('keydown', (e) => {
          if (lightbox.classList.contains('hidden')) return;
          if (e.key === 'Escape') closeLightbox();
          if (e.key === 'ArrowRight') nextImage();
          if (e.key === 'ArrowLeft') prevImage();
        });
      });
    })();
  </script>
</Layout>
